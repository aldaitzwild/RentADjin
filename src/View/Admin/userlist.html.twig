<div class='container'>

    <h1> Liste des Utilisateurs</h1>

    {% for user in users %}
        <ul>
            <li>{{ user.lastname }}.{{ user.firstname }} </li>
        </ul>
    {% endfor %}

</div>

