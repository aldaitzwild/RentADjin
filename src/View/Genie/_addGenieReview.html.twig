
{# {% if userInfo.id is defined%} #}

  <p>Qu'avez vous pensé de {{genieInfo.genieName}} ?</p>

{% if errorsReview is not empty %} 
      <ul class="errorsList">
				{% if errorsReview.input is defined %}
					<li>
						{{errorsReview.input}}
					</li>
				{% endif %}
			</ul>
{% endif %}


  <form action="/genies/review" method="post">
    {# <div class="form-group"> #}
      
      <input type="hidden" id="genie" name="genie" value={{genieInfo.genieId}} class="form-control">
    {# </div> #}

    {# <div class="form-group"> #}
    
      <input type="hidden" id="userId" name="userId" value={{userInfo.id}} class="form-control">
    {# </div> #}

    <div class="form-group m-2">
      <label for="rating">Note</label>
      <input type="number" id="rating" name="rating" min="0" max="5" class="form-control">
    </div>

    <div class="form-group m-2">
      <label for="review">Votre avis</label>
      <textarea class="form-control" id="review" name="review" rows="3" cols=""></textarea>
    </div>
    <button class="btn sendReview m-2" type="submit">Envoyer</button>
  </form>

</div>

{# {% endif %} #}